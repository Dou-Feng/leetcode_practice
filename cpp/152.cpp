#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    int maxProduct(vector<int>& nums) {
        int ans = nums[0];
        int accum = nums[0];
        int i = 1, j = 1;
        while (j < n) { 
            
        }
        
    }
};

int main() {
    Solution sol;
    vector<int> input({2,-5,-2,-4,3});
    int s = sol.maxProduct(input);
    cout << s << endl;
    return 0;
}